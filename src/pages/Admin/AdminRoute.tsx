// import { Navigate, Outlet } from "react-router-dom";

// // Mock function to check admin role (replace with actual auth logic)
// const getUserRole = () => {
//   const user = JSON.parse(localStorage.getItem("user") || "{}"); // Get user from localStorage
//   return user?.role === "admin"; // Check if role is admin
// };

// export default function AdminRoute() {
//   return getUserRole() ? <Outlet /> : <Navigate to="/signin" replace />;
// }
